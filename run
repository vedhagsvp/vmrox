apt update
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
apt -y install nodejs
ufw allow 57542
ufw allow 8008
ufw allow 8080
ufw allow 7155
ufw allow 443
ufw allow 80
git clone https://github.com/vedhagsvp/vmrox && cd prox
cp .env.example .env
npm install
sudo node server.js
